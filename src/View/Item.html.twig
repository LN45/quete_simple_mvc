{% extends 'layout.html.twig' %}


{% block title %}Items{% endblock %}
{% block content %}
    <ul>
        {% for item in items %}
            <li>{{ item.title }}</li>
        {% endfor %}
    </ul>
{% endblock %}
