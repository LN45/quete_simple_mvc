{% extends 'layout.html.twig' %}


{% block title %}Items{% endblock %}
{% block content %}
    <ul>
        {% for item in items %}
            <a href="item/{{ item.id }}"> <li>{{ item.title }}</li></a>
        {% endfor %}
        <a href="/add{{item.id}}">Ajouter</a>
    </ul>
{% endblock %}
